# Full local development stack: bronze ingestion, dbt transforms, FastAPI, the
# decision agent, dashboard, and tests.
#
# Kept separate from requirements.txt (root), which is intentionally slim — that file
# is what Streamlit Community Cloud installs for the deployed dashboard, and it must
# stay lightweight to keep Cloud cold-start/build times low. See DECISIONS.md
# ("Phase 1 — the Streamlit Cloud sample-data path") for the full rationale.
#
# Usage:
#   pip install -r requirements-dev.txt

# Data layer
duckdb>=1.5.0
pandas>=2.2.0
pyarrow>=15.0.0
python-dotenv>=1.0.0

# dbt
dbt-core>=1.11.0
dbt-duckdb>=1.10.0

# UN Comtrade / World Bank ingestion
requests>=2.31.0
httpx>=0.27.0

# Decision agent (direct Anthropic SDK — see agent/decision_agent.py)
anthropic>=0.40.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0

# AWS (S3 data-layer export/fetch — see scripts/export_to_s3.py, docker/fetch_db.py)
boto3>=1.34.0

# Dev tooling
pytest>=8.2.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
moto[s3]>=5.0.0
responses>=0.25.0
