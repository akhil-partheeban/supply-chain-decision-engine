# Runtime dependencies for the containerized FastAPI service (docker/Dockerfile.api).
# Deliberately narrower than requirements-dev.txt: no dbt, no pytest/ruff, nothing
# needed only at development time. A smaller image means a faster cold start on
# AWS App Runner / Fargate, which bills (and cold-starts) per container.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
duckdb>=1.5.0
pandas>=2.2.0
python-dotenv>=1.0.0
anthropic>=0.40.0

# S3 data-layer fetch on container startup (see docker/fetch_db.py)
boto3>=1.34.0
