# Streamlit Community Cloud deployment ONLY — kept intentionally slim (see
# DECISIONS.md, Phase 1, "the Streamlit Cloud sample-data path") so Cloud installs
# stay fast. streamlit_app.py bootstraps a synthetic DuckDB via data/sample_data.py
# when no real database is present, so this file never needs dbt/FastAPI/the agent.
#
# For full local development (bronze ingestion, dbt, FastAPI, the decision agent,
# tests) use requirements-dev.txt instead:
#   pip install -r requirements-dev.txt
streamlit>=1.35.0
plotly>=5.22.0
duckdb>=1.5.0
pandas>=2.2.0
python-dotenv>=1.0.0
requests>=2.31.0
